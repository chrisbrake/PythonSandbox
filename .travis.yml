sudo: false

notfications:
  email:
    on_success: always
  irc:
    on_success: always
    on_failure: always
    use_notice: false
    skip_join: false
    channels:
      - "chat.freenode.net#cignature"

language: python

python:
  - 3.6

install:
  - pip install -r requirements.txt

script:
  - python -m unittest discover